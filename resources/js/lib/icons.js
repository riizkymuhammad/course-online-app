import { Languages, FileText, School, GraduationCap, BookOpen } from "lucide-react"

export const iconMap = {
  languages: Languages,
  cpns: FileText,
  school: School,
  graduation: GraduationCap,
  book: BookOpen,
}
